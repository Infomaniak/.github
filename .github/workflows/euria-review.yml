name: Euria Review

on: workflow_call

jobs:
  ai-review:
    runs-on: ubuntu-latest
    
    steps:
    - name: PR AI review combined steps
      uses: qodo-ai/pr-agent@1b0609a013f53694c36d457149bde70abf50c048 # V0.32
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        OPENAI_KEY: ${{ secrets.EURIA_API_KEY }}
        OPENAI_API_BASE: "https://api.infomaniak.com/2/ai/27/openai/v1"
        CONFIG__MODEL: "openai/moonshotai/Kimi-K2.5"
        CONFIG__CUSTOM_MODEL_MAX_TOKENS: 256000
        PR_CODE_SUGGESTIONS__COMMITABLE_CODE_SUGGESTIONS: "true"
        PR_CODE_SUGGESTIONS__DUAL_PUBLISHING_SCORE_THRESHOLD: "7"
        
        github_action_config.auto_review: "true"
        github_action_config.auto_describe: "false"
        github_action_config.auto_improve: "true"
        github_action_config.pr_actions: '["opened","reopened","ready_for_review","synchronize"]'